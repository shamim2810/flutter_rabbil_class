import 'package:flutter/material.dart';

class SettingFragmention extends StatelessWidget {
  const SettingFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SettingFragmention'),
      ),
    );
  }
}