import 'package:flutter/material.dart';

class PersonFragmention extends StatelessWidget {
  const PersonFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PersonFragmention'),
      ),
    );
  }
}