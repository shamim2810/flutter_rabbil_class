import 'package:flutter/material.dart';

class AlarmFragmention extends StatelessWidget {
  const AlarmFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('AlarmFragmention'),
      ),
    );
  }
}
