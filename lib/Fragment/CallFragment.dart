import 'package:flutter/material.dart';

class CallFragmention extends StatelessWidget {
  const CallFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CallFragmention'),
      ),
    );
  }
}