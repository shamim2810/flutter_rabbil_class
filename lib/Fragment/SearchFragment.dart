import 'package:flutter/material.dart';

class SearchFragmention extends StatelessWidget {
  const SearchFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SearchFragmention'),
      ),
    );
  }
}