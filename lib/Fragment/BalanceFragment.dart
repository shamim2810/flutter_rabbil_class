import 'package:flutter/material.dart';

class BalanceFragmention extends StatelessWidget {
  const BalanceFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('BalanceFragmention'),
      ),
    );
  }
}