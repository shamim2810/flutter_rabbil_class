import 'package:flutter/material.dart';

class PhotoFragmention extends StatelessWidget {
  const PhotoFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PhotoFragmention'),
      ),
    );
  }
}