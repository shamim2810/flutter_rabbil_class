import 'package:flutter/material.dart';

class HomeFragmention extends StatelessWidget {
  const HomeFragmention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HomeFragmention'),
      ),
    );
  }
}